"use client";

import React from "react";

export function Providers({ children }: { children: React.ReactNode }) {
  // Placeholder for global providers (Auth, Theme, Toast, etc.)
  return <>{children}</>;
}

export default Providers;
